<?php
/**
 * Fired when the plugin is uninstalled.
 *
 * @package WooPilot_Bale
 */

defined( 'WP_UNINSTALL_PLUGIN' ) || exit;

// For now, keep plugin data for safety.
// Later we can add a setting: "Delete all data on uninstall".